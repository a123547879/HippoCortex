# 小白大脑类脑认知系统

# 架构设计说明书

**Architecture Design Specification**

---

| 项目 | 内容 |
|------|------|
| 文档版本 | V1.0 |
| 编制日期 | 2026-05-12 |
| 编制人 | AI Assistant |
| 密级 | 内部公开 |

---

## 目录

1. [引言](#1-引言)
2. [系统概述](#2-系统概述)
3. [总体架构设计](#3-总体架构设计)
4. [核心模块详细设计](#4-核心模块详细设计)
5. [数据模型设计](#5-数据模型设计)
6. [数据流设计](#6-数据流设计)
7. [接口设计](#7-接口设计)
8. [配置参数设计](#8-配置参数设计)
9. [外部依赖与部署](#9-外部依赖与部署)
10. [附录](#10-附录)

---

## 1. 引言

### 1.1 编写目的

本文档旨在全面描述"小白大脑"类脑认知系统的软件架构设计，为开发、测试、运维人员提供技术参考，确保系统各模块的设计意图、交互关系和数据流转清晰可追踪。

### 1.2 适用范围

本文档适用于以下场景：
- 系统开发与迭代
- 模块集成与接口对接
- 性能优化与故障排查
- 新成员技术 onboarding

### 1.3 术语与缩略语

| 术语 | 英文全称 | 说明 |
|------|----------|------|
| SDR | Sparse Distributed Representation | 稀疏分布式表征，模拟大脑皮层稀疏编码 |
| STDP | Spike-Timing-Dependent Plasticity | 脉冲时序依赖可塑性，生物突触学习规则 |
| RPE | Reward Prediction Error | 奖励预测误差，多巴胺系统核心指标 |
| HTM | Hierarchical Temporal Memory | 层级时间记忆理论，Numenta公司提出 |
| VAE | Variational Autoencoder | 变分自编码器，用于图像潜在空间编解码 |
| CLIP | Contrastive Language-Image Pre-training | OpenAI多模态对比学习模型 |
| LLM | Large Language Model | 大语言模型，本系统使用Ollama本地部署 |
| KG | Knowledge Graph | 知识图谱，极简实体-记忆双向映射 |
| FAISS | Facebook AI Similarity Search | 高效向量相似性搜索库 |

---

## 2. 系统概述

### 2.1 设计背景与目标

当前主流大语言模型存在以下局限：
- **无持久记忆**：每次对话独立，无法积累长期知识
- **无生物节律**：持续运行无疲劳/睡眠机制
- **无结构化学习**：缺乏类似人脑的突触可塑性机制
- **无多模态融合**：文本与视觉信息隔离处理

本系统旨在构建一个**生物合理性（Biologically Plausible）**的类脑认知架构，模拟人类大脑的记忆编码、巩固、遗忘、睡眠、走神等核心认知功能。

### 2.2 核心设计理念

| 设计原则 | 具体实现 |
|----------|----------|
| **稀疏编码** | SDR 2048维仅2-5%激活，模拟大脑皮层稀疏性 |
| **局部连接** | 专家网络内核心功能区强化连接，跨区衰减 |
| **突触可塑性** | STDP双向调节，支持LTP增强与LTD抑制 |
| **能量驱动** | 认知能量场量化系统混乱度，驱动状态切换 |
| **多模态融合** | CLIP视觉+Ollama文本+VAE图像统一表征 |
| **类人节律** | 疲劳积累→睡眠触发→三阶段巩固→梦境生成 |

### 2.3 核心特性清单

- ✅ 五大脑区专家网络（身份/概念/空间/抽象/视觉）
- ✅ 稀疏分布式表征（SDR）编码与解码
- ✅ 脉冲时序依赖可塑性（STDP）学习
- ✅ 三阶段睡眠巩固机制（浅睡/深睡/REM）
- ✅ 认知能量场与疲劳管理系统
- ✅ 艾宾浩斯遗忘曲线对话记忆
- ✅ 多模态图文融合学习
- ✅ 零硬编码符号知识提取
- ✅ 记忆冲突检测与自动失效
- ✅ 主动意图生成与社交互动

---

## 3. 总体架构设计

### 3.1 分层架构

系统采用**七层分层架构**，自顶向下依次为：

```
┌─────────────────────────────────────────────┐
│  Layer 1: 用户交互层 (User Interaction)      │
│  brain_dashboardV10.py | ChatThread.py      │
│  Multimodal_gateway.py | VAEManager.py      │
├─────────────────────────────────────────────┤
│  Layer 2: 系统入口层 (System Entry)          │
│  brain_interface.py | brain_core.py         │
│  MainTest5.py                               │
├─────────────────────────────────────────────┤
│  Layer 3: 认知系统层 (Cognitive System)      │
│  Cognitive_systemV2.py + 四大认知循环        │
├─────────────────────────────────────────────┤
│  Layer 4: 记忆存储层 (Memory Storage)        │
│  HippocampusRouter | PersistentCortex       │
│  DynamicExpert × 5 | LearnableSparseEncoder │
├─────────────────────────────────────────────┤
│  Layer 5: 神经科学层 (Neuroscience)          │
│  Thalamus | SymbolicCore | CognitiveEnergy  │
│  DopamineSystem | Metacognition | Curiosity │
├─────────────────────────────────────────────┤
│  Layer 6: 基础设施层 (Infrastructure)        │
│  EventBus | PluginSystem | BrainConfig      │
│  DataModels | DummyBrain | ConversationMem  │
├─────────────────────────────────────────────┤
│  Layer 7: 外部依赖层 (External Dependencies) │
│  Ollama LLM | PyTorch/FAISS | CLIP | VAE    │
└─────────────────────────────────────────────┘
```

### 3.2 模块关系图

```
                    ┌─────────────┐
                    │   用户输入   │
                    │ (文本/图像)  │
                    └──────┬──────┘
                           │
           ┌───────────────┼───────────────┐
           ▼               ▼               ▼
    ┌────────────┐  ┌────────────┐  ┌────────────┐
    │  LLM封装   │  │多模态网关  │  │  VAE编码   │
    │(问答生成)  │  │(CLIP融合)  │  │(图像压缩)  │
    └─────┬──────┘  └─────┬──────┘  └─────┬──────┘
          │               │               │
          └───────────────┼───────────────┘
                          ▼
                   ┌────────────┐
                   │    丘脑    │
                   │(过滤/中继) │
                   └─────┬──────┘
                         │
           ┌─────────────┼─────────────┐
           ▼             ▼             ▼
    ┌────────────┐ ┌────────────┐ ┌────────────┐
    │ 海马体路由 │ │ 符号核心   │ │ 认知能量场 │
    │(专家分类)  │ │(三元组提取)│ │(混乱度计算)│
    └─────┬──────┘ └─────┬──────┘ └─────┬──────┘
          │              │              │
          ▼              │              ▼
    ┌────────────┐       │       ┌────────────┐
    │ SDR编码器  │       │       │  疲劳积累  │
    │(稀疏编码)  │       │       │  状态切换  │
    └─────┬──────┘       │       └─────┬──────┘
          │              │             │
          └──────────────┼─────────────┘
                         ▼
              ┌────────────────────┐
              │   五大脑区专家网络  │
              │ 身份/概念/空间/抽象/视觉│
              │   (STDP突触可塑性)   │
              └─────────┬──────────┘
                        │
          ┌─────────────┼─────────────┐
          ▼             ▼             ▼
   ┌────────────┐ ┌────────────┐ ┌────────────┐
   │ 皮层长期记忆│ │ 关联图谱   │ │ 知识图谱   │
   │(FAISS索引) │ │(认知突触)  │ │(实体映射)  │
   └────────────┘ └────────────┘ └────────────┘
```

### 3.3 设计模式应用

| 模式 | 应用位置 | 说明 |
|------|----------|------|
| **单例模式** | BrainCore, EventBus | 确保全局唯一实例 |
| **策略模式** | 五大脑区专家 | 统一接口，差异化实现 |
| **观察者模式** | EventBus | 发布/订阅异步事件 |
| **工厂模式** | PluginManager | 动态加载感知/动作插件 |
| **代理模式** | DummyBrain | 兼容层封装，屏蔽内部复杂度 |
| **模板方法** | 四大认知循环 | 统一流程，子类差异化实现 |

---

## 4. 核心模块详细设计

### 4.1 BrainConfig.py - 全局配置中心

**模块定位**：系统的"基因编码"，集中管理所有可调参数。

**核心配置项**：

| 配置类别 | 关键参数 | 默认值 | 说明 |
|----------|----------|--------|------|
| 向量维度 | `dim` | 1024 | bge-m3原生维度 |
| SDR维度 | `sdr_dim` | 2048 | 稀疏表征维度 |
| SDR激活数 | `sdr_active_size` | 60 | 约2.9%激活率 |
| 设备 | `device` | 'cpu' | 计算设备 |
| 专家名称 | `expert_names` | [身份,概念,空间,抽象,视觉] | 五大脑区 |
| 检索TopK | `top_k` | 10 | 单次检索返回数 |
| 最小相似度 | `min_similarity` | 0.4 | 记忆匹配阈值 |
| 去重阈值 | `duplicate_threshold` | 0.92 | 语义重复判定 |
| 永久记忆阈值 | `permanent_importance_threshold` | 0.9 | 永久记忆重要性门槛 |
| 遗忘天数 | `forget_days` | 365 | 自然遗忘周期 |
| LLM模型 | `ollama_model_name` | "gemma3:4b" | 本地大模型 |
| 温度 | `llm_temperature` | 0.3 | 生成随机性 |
| 最大Token | `llm_max_tokens` | 256 | 单次生成长度 |
| 存储目录 | `storage_dir` | "brain_v12_demo" | 数据持久化路径 |
| VAE路径 | `YOUR_VAE_MODEL_PATH` | 本地路径 | SD-VAE-ft-mse模型 |

**专家差异化配置（EXPERT_CONFIG）**：

| 专家 | 稀疏度 | 局部半径 | 核心偏置 | SDR激活数 | STDP特性 |
|------|--------|----------|----------|-----------|----------|
| 身份 | 5% | 0.1 | 0.8 | 15 | τ⁺=25, A⁺=0.015（强记忆保持） |
| 概念 | 2% | 0.2 | 0.5 | 20 | 平衡LTP/LTD |
| 空间 | 3% | 0.35 | 0.4 | 18 | 大范围连接 |
| 抽象 | 2.5% | 0.25 | 0.45 | 16 | 中等范围 |
| 视觉 | 4% | 0.15 | 0.55 | 14 | 视觉特征专用 |

**局部连接偏置配置**：
- `local_bias_enabled`: True（启用局部连接）
- `local_bias_strength`: 1.2（同分区连接强化系数）
- `cross_partition_decay`: 0.7（跨分区衰减系数）

**走神状态配置**：
- `mind_wandering_enabled`: True
- `mind_wandering_idle_threshold`: 30秒（空闲触发时间）
- `fatigue_sleep_threshold`: 0.85（疲劳睡眠阈值）
- `mind_wandering_recall_prob`: 0.5（记忆闪回概率）

**对话记忆配置**：
- `normal_decay_lambda`: 0.1（普通对话半衰期≈7小时）
- `important_decay_lambda`: 0.02（重要对话半衰期≈35小时）
- `forget_threshold`: 0.1（遗忘阈值）
- `max_context_turns`: 8（最大上下文轮数）

---

### 4.2 brain_core.py - 大脑核心

**模块定位**：系统全局状态管理中心，采用单例模式确保唯一性。

**类设计**：

```python
class BrainCore:
    _instance = None      # 单例实例
    _lock = threading.Lock()  # 线程安全锁

    # 全局状态属性
    fatigue_level: float = 0.0           # 疲劳度 [0,1]
    is_mind_wandering: bool = False      # 走神状态
    needs_sleep_request: bool = False    # 睡眠请求标志
    fatigue_sleep_threshold: float = 0.85 # 睡眠触发阈值
    last_interaction_time: datetime      # 最后交互时间戳
    interaction_event: threading.Event   # 交互事件信号

    # 认知能量场实例
    energy_field: CognitiveEnergyField

    # 状态存储字典
    _state: Dict[str, Any]
```

**核心方法**：

| 方法 | 功能 | 调用时机 |
|------|------|----------|
| `start(storage_dir)` | 初始化存储目录，启动能量场 | 系统启动 |
| `stop()` | 停止运行，关闭能量场 | 系统退出 |
| `update_interaction_time()` | 更新最后交互时间，重置走神 | 每次用户输入 |
| `get_state(key)` | 获取全局状态 | 各模块读取 |
| `set_state(key, value)` | 设置全局状态 | 各模块写入 |

**线程安全**：所有状态访问通过`_lock`互斥锁保护，确保多线程安全。

---

### 4.3 Cognitive_systemV2.py - 认知系统整合器

**模块定位**：四大认知循环的中央调度器，负责组件初始化和生命周期管理。

**组件初始化顺序**：

```
1. 专家网络初始化 (DynamicExpert × 5)
   └─ 加载差异化配置 (稀疏度/半径/偏置/STDP参数)

2. SDR编码器初始化 (LearnableSparseEncoder × 5)
   └─ 加载历史权重或初始化新编码器

3. 皮层初始化 (PersistentCortex)
   └─ 加载FAISS索引、记忆图谱、对话历史

4. 海马体路由初始化 (HippocampusRouter)
   └─ 加载路由网络权重，初始化专家原型向量

5. 符号核心初始化 (SymbolicCore)
   └─ 初始化实体神经元分配器

6. VAE管理器初始化 (VAEManager)
   └─ 配置模型路径，懒加载策略

7. 丘脑初始化 (Thalamus)
   └─ 绑定海马体/皮层/能量场/专家

8. 类人学习模块初始化
   └─ 多巴胺系统 / 元认知 / 好奇心

9. 四大循环组件绑定
   └─ 感知/学习/巩固/梦境循环注入组件引用

10. 记忆衰减执行
    └─ 调用皮层decay_all_memories()
```

**对外统一接口**：

| 接口方法 | 实际调用 | 功能 |
|----------|----------|------|
| `think(text)` | PerceptionLoop.think() | 思考推理 |
| `learn(text)` | LearningLoop.learn() | 学习记忆 |
| `batch_learn(texts)` | LearningLoop.batch_init_direct_to_cortex() | 批量导入 |
| `sleep_consolidate_all()` | ConsolidationLoop.sleep_consolidate_all() | 睡眠巩固 |
| `generate_dream()` | DreamingLoop.generate_dream() | 梦境生成 |
| `get_brain_status()` | PerceptionLoop.get_brain_status() | 状态查询 |

---

### 4.4 Perception_loop.py - 感知循环

**模块定位**：处理用户输入的核心推理引擎，负责记忆检索、激活传播和结果生成。

**主流程 - think() 方法**：

```
输入: text (用户查询)
输出: ThoughtResult (思考结果对象)

Step 1: 更新交互时间，发射INPUT_RECEIVED事件
Step 2: 获取时间衰减对话上下文
        └─ 调用 cortex.get_active_conversation_context()
        └─ 构建上下文提示文本
Step 3: 初始化和路由
        └─ 文本编码 (bge-m3 → 1024维)
        └─ 归一化
        └─ 海马体路由分类 (5大专家)
        └─ 本地规则兜底 (视觉/身份关键词)
        └─ 计算认知能量
        └─ SDR编码 (2048维稀疏)
        └─ 符号检索上下文构建
Step 4: 记忆检索
        └─ 丘脑调度检索 (海马体+皮层)
        └─ 动态相似度阈值 (基于能量)
        └─ 构建全局记忆池
Step 5: 记忆融合与过滤
        └─ 专家网络激活传播 (2-3步)
        └─ 预测下一个SDR
        └─ 计算预测误差
        └─ 融合分数计算 (全局+专家+交叉验证)
        └─ 视觉记忆链接 (绑定ID匹配)
Step 6: 构建思考链
        └─ 提取查询逻辑结构
        └─ 构建符号逻辑链
        └─ 提取核心思想
Step 7: 返回ThoughtResult
```

**动态阈值机制**：

认知能量越低（系统越稳定），检索阈值越宽松，允许更多记忆参与；能量越高（越混乱），阈值越严格，只保留高置信度记忆。

| 总能量 | 动态最小相似度 | 说明 |
|--------|----------------|------|
| < 2 | 0.05 | 系统稳定，宽松检索 |
| 2-5 | 0.10 | 正常状态 |
| > 5 | 0.25 | 系统混乱，严格过滤 |

**走神检测**：
- 每2秒检查一次空闲时间
- 空闲超过30秒 → 触发走神
- 走神期间：记忆闪回 + 联想想象 + 疲劳积累 + 意图生成

---

### 4.5 Learning_loop.py - 学习循环

**模块定位**：负责新记忆的编码、存储和强化学习。

**主流程 - learn() 方法**：

```
输入: text (学习内容), force_expert (强制专家), external_reward (外部奖励)
输出: mem_id (记忆ID) 或 None

Step 1: 陈述句检测
        └─ 疑问句/查询句 → 返回None（不学习）
        └─ 陈述句 → 继续
Step 2: 文本编码 → 1024维CLIP向量 → 归一化
Step 3: 丘脑过滤与中继
        └─ 计算显著性
        └─ 关键信息强制放行（身份/高重要性/待巩固）
        └─ 低显著性信息过滤
Step 4: 专家路由
        └─ 有force_expert → 直接使用
        └─ 无 → 海马体路由自动分类
Step 5: SDR编码
        └─ 对应专家编码器生成2048维稀疏向量
Step 6: 元认知预评估
        └─ 计算先验置信度
Step 7: 海马体快速编码
        └─ 模式分离（相似记忆差异化）
        └─ 写入临时缓冲区
Step 8: 符号学习
        └─ 提取三元组（主体-谓词-对象）
Step 9: 视觉接地
        └─ 非视觉专家 → 检索视觉记忆建立弱连接
Step 10: 多巴胺强化
        └─ 计算信息增益（好奇心奖励）
        └─ 计算预测奖励（显著性）
        └─ 总奖励 = 外部 + 内部
        └─ 计算RPE
        └─ 调节相关突触权重
Step 11: 元认知后评估
        └─ 更新置信度
        └─ 触发好奇心提问（如需要）
```

**批量导入优化**：
- 路径：`batch_init_direct_to_cortex()`
- 特点：跳过丘脑/海马体/多巴胺/元认知，直接写入皮层
- 用途：冷启动知识库灌入
- 批次：每批32条

**突触连接管理**：
- 存储格式：`{(from_mem_id, to_mem_id): weight}`
- 双向连接：正向权重 + 反向权重×0.8
- 持久化：JSON格式存储

---

### 4.6 Consolidation_loop.py - 巩固循环

**模块定位**：睡眠期间执行记忆固化、突触重塑和知识整合。

**三阶段睡眠流程**：

#### 阶段一：浅睡（Light Sleep）
- **功能**：整理重要对话，巩固到抽象专家
- **输入**：`cortex.get_pending_conversation_consolidation()`
- **处理**：将重要对话编码为记忆，写入皮层
- **输出**：`important_conversations_consolidated` 计数

#### 阶段二：深睡（Deep Sleep）
- **功能**：核心记忆巩固与突触强化
- **子流程**：
  1. 元认知优先级评估
     - 计算每条记忆的复习优先级
     - 公式：`0.4×(1-置信度) + 0.35×重要性 + 0.25×新近度`
  2. 丘脑协调巩固
     - 高优先级记忆优先回放
     - 重要性低于阈值 → 遗忘
  3. 海马体缓冲区固化
     - 临时记忆 → 皮层长期记忆
  4. 多巴胺离线重放
     - 对高优先级记忆模拟奖励
     - 强化相关突触
  5. 专家睡眠巩固
     - 每个专家执行`sleep_consolidate()`
     - 符号-神经绑定强化

#### 阶段三：REM睡眠
- **功能**：记忆整合与梦境生成
- **处理**：
  1. 收集各专家梦境片段
  2. 融合高优先级记忆
  3. LLM生成连贯梦境故事
  4. 梦境作为新记忆存入抽象专家

**睡眠质量评分**（0-100分）：

| 指标 | 权重 | 计算方式 |
|------|------|----------|
| 巩固率 | 40% | 巩固数/总处理数 |
| 遗忘控制 | 30% | 1 - 遗忘率 |
| 能量效率 | 20% | max(0, 20 - 能量消耗×0.5) |
| 阶段完整性 | 10% | REM梦境 + 专家巩固奖励 |

---

### 4.7 Dreaming_loop.py - 梦境循环

**模块定位**：模拟REM睡眠期间的梦境生成过程。

**梦境生成流程**：

```
Step 1: 随机选择1-2个主导专家
Step 2: 对每个主导专家：
        a. 生成随机初始激活（5%神经元随机放电）
        b. 突触传播2步（模拟联想链条）
        c. 每步加入噪声（模拟梦境荒诞性）
        d. 检索激活对应的记忆
Step 3: 提取Top活跃记忆片段
Step 4: 去重，构建梦境碎片列表
Step 5: LLM生成式重构
        提示词要求：
        - 自然融合所有片段
        - 有开头/发展/结尾
        - 加入超现实元素
        - 去除技术标签
        - 不超过200字
Step 6: 梦境作为新记忆存入
Step 7: 返回DreamResult对象
```

**全局梦境**：融合所有专家梦境 + 高优先级记忆，生成统一梦境叙事。

---

### 4.8 HippocampusRouterV10.py - 海马体路由器

**模块定位**：负责信息分类路由、快速编码和临时记忆管理。

**路由网络架构**：

```
输入: 1024维CLIP向量
      ↓
Linear(1024 → 256)
LayerNorm(256)
GELU激活
      ↓
Linear(256 → 5)  [对应5大专家]
Softmax → 概率分布
      ↓
语义网络得分融合 (40%NN + 60%语义)
      ↓
输出: 目标专家名称 + 置信度
```

**在线学习机制**：
- 原型向量微调：每次路由后更新对应专家原型
- 训练缓冲：维护2000条最近样本
- 平衡训练：每16/32条样本执行一次AdamW优化

**模式分离（Pattern Separation）**：
- 检测海马体缓冲区内相似记忆（相似度>0.85）
- 翻转10%的SDR位，避免记忆混淆
- 模拟海马体齿状回（Dentate Gyrus）的功能

**模式补全（Pattern Completion）**：
- 部分输入向量 → 检索最相似的完整记忆
- 模拟海马体CA3区的自联想功能

---

### 4.9 PersistentCortexV16.py - 持久皮层

**模块定位**：长期记忆的存储、索引、检索和管理中心。

**MemoryIndex 索引架构**：

```
MemoryIndex
├── memories: Dict[int, MemoryPacket]
│   └── 核心记忆存储（SDR + CLIP向量 + 内容 + 元数据）
├── faiss_index: IndexIDMap
│   └── FAISS向量索引（1024维，内积相似度）
├── association_graph: Dict[int, List[(int, float)]]
│   └── 记忆关联图谱（认知突触权重）
├── expert_index: Dict[str, List[int]]
│   └── 按专家分类的记忆索引
├── semantic_index: Dict[str, List[int]]
│   └── 按语义标签索引
├── subject_index: Dict[str, List[int]]
│   └── 按主体索引
├── entity_index: Dict[str, List[int]]
│   └── 按实体索引
└── tag_index: Dict[str, List[int]]
    └── 按标签索引
```

**记忆存储流程**：

```
输入: expert_name, sdr, clip_vec, content, metadata

Step 1: 结构化信息提取
        └─ 主体/谓词/对象三元组
        └─ 语义标签自动提取
Step 2: 冲突检测
        └─ 同主体+同属性记忆检测
        └─ 语义相似度>0.85 → 标记旧记忆失效
        └─ 关键词规则保险（否定词/反义词检测）
Step 3: 去重检测
        └─ FAISS向量搜索Top1
        └─ 相似度>0.92 → 更新访问时间，返回旧ID
Step 4: 写入索引
        └─ FAISS索引添加向量
        └─ 所有二级索引更新
Step 5: 专家网络更新
        └─ STDP/赫布学习更新突触
        └─ 记忆存入专家SDR列表
Step 6: 关联构建
        └─ 同标签记忆建立关联（强度0.3）
        └─ 同专家记忆建立关联（强度0.1）
Step 7: 知识图谱映射
        └─ 提取实体
        └─ 建立实体-记忆双向边
```

**对话记忆系统**：
- 艾宾浩斯指数衰减：`activation = initial × exp(-λ×t)`
- 差异化衰减：普通λ=0.1，重要λ=0.02
- 自动清理：每10轮对话清理完全遗忘记录
- 上下文注入：按激活值排序取Top8轮

---

### 4.10 DynamicExpertV8.py - 动态专家网络

**模块定位**：模拟大脑皮层功能区的稀疏神经网络，是系统的核心计算单元。

**网络结构**：

```
SDR输入 (2048维, 稀疏)
    ↓
突触权重矩阵 (2048 × 2048)
    ├─ 局部连接偏置初始化
    ├─ 核心功能区强化 (前20%)
    └─ 差异化稀疏度掩码
    ↓
SNN脉冲衰减 (时间维度)
    ↓
Sigmoid激活 + TopK稀疏化
    ↓
输出SDR (2048维, 稀疏)
```

**STDP更新算法**：

```python
def stdp_update(pre_sdr, post_sdr, delta_t=0.0):
    # 记录脉冲历史 (timestep, neuron_id, is_pre)
    record_spikes(pre_sdr, is_pre=True)
    record_spikes(post_sdr, is_pre=False)

    # 计算时间差
    for pre_t, pre_n in pre_spikes:
        for post_t, post_n in post_spikes:
            dt = post_t - pre_t
            if dt > 0:
                # 突触前先发放 → LTP
                delta_w[post_n, pre_n] += A_plus * exp(-dt / tau_plus)
            elif dt < 0:
                # 突触后先发放 → LTD
                delta_w[post_n, pre_n] -= A_minus * exp(dt / tau_minus)

    # 应用更新
    synapse += learning_rate * delta_w
    synapse = clamp(synapse, -1.0, 1.0)
```

**突触修剪（Synaptic Pruning）**：
- 目标：维持10-15%稀疏度
- 策略：删除重要性最低10%的突触
- 重要性 = |权重| × log(1 + 更新次数)

**突触新生（Synaptogenesis）**：
- 条件：高激活神经元 + 高共激活频率 + 当前无连接
- 初始权重：0.1 × (1 + 共激活次数/10)
- 上限：最大密度15%

**预测编码**：
- 预测头：`Linear → LayerNorm → GELU → Linear`
- 预测下一个SDR
- MSE损失驱动轻量更新（无反向传播，纯数值更新）

---

### 4.11 LearnableSparseEncoderV2.py - 可学习稀疏编码器

**模块定位**：将密集向量转换为稀疏分布式表征（SDR）。

**编码器架构**：

```
输入向量 (1024维)
    ↓
Linear(1024 → 4096)
LayerNorm(4096)
GELU
    ↓
Linear(4096 → 2048)
    ↓
竞争激活层:
  ├─ 核心功能区偏置 (前20%维度 + core_bias)
  ├─ 侧抑制: softmax(lateral_inhibition) × activation
  ├─ TopK硬选择
  └─ 直通估计器: hard_mask - softmax.detach() + softmax
    ↓
输出SDR (2048维, 仅TopK位非零)
```

**解码器架构**：

```
SDR (2048维)
    ↓
Linear(2048 → 1024)
LayerNorm(1024)
GELU
    ↓
Linear(1024 → 1024)
    ↓
重构向量 (1024维)
```

**训练目标**：
- 重构损失（MSE）最小化
- 稀疏性约束（TopK竞争）
- 在线学习：缓冲32条样本后批量训练

---

### 4.12 Thalamus.py - 丘脑模块

**模块定位**：信息过滤、注意力调控和记忆巩固协调。

**信息过滤算法**：

```python
def filter_and_relay(input_vec, input_text, metadata):
    # 强制放行条件
    if is_identity_info or is_important or is_pending_consolidate:
        return True, enhanced_packet(saliency=1.0)

    # 显著性计算
    saliency = 0.5
    saliency += novelty × 0.4          # 新颖性
    saliency += entity_match × 0.2     # 实体匹配
    saliency += importance × 0.3       # 元数据重要性
    saliency *= arousal_level          # 觉醒水平

    # 动态阈值
    threshold = base_threshold + energy_factor
    if brain_state == "wandering":
        threshold += 0.2

    if saliency < threshold:
        return False, {}  # 过滤

    # 注意力锁定
    if saliency > 0.8:
        current_attention_focus = input_text

    return True, packet
```

**状态切换**：

| 状态 | 觉醒水平 | 注意力阈值 | 说明 |
|------|----------|------------|------|
| 清醒 | 1.0 | 0.3 | 正常交互 |
| 走神 | 0.6 | 0.5 | 降低注意力 |
| 睡眠 | 0.2 | 0.8 | 仅巩固高显著记忆 |

---

### 4.13 SymbolicCore.py - 符号核心

**模块定位**：零硬编码的符号知识提取与推理系统。

**实体神经元分配**：
- 每个新实体分配25个专属神经元
- 起始位置：第500维（前500维保留给系统）
- 自动扩展，上限2048维

**三元组学习**：
- 从文本自动提取（主体, 谓词, 对象）
- 规则模式："X是Y"、"X喜欢Y"、"X住在Y"等
- 存储结构：`{(subject, predicate): [objects]}`

**指代消解学习**：
- 从对话中学习："我叫邓尧" → "我" = "邓尧"
- 从对话中学习："你叫小白" → "你" = "小白"
- 最近实体推断："他" → 最近提到的人名

**问句解析**：
- 分词（jieba）
- 实体匹配（解析指代后）
- 属性匹配（从已学习属性中找）
- 意图识别

---

### 4.14 CognitiveEnergyField.py - 认知能量场

**模块定位**：量化系统认知状态的混乱度指标。

**七维能量计算**：

```python
def total_energy(routing_probs, triple_scores, sim_scores, 
                 rule_match, synapse_change, is_wandering, fatigue_level):

    e_neural = neural_energy(routing_probs)      # 路由熵（概率分布混乱度）
    e_symbol = len(triple_scores) × 0.1          # 符号负载
    e_retriev = (1 - mean(sim_scores)) × 2.0     # 检索混乱度
    e_rule = 0.0 if rule_match else 1.5          # 规则冲突
    e_synap = synapse_change × 0.001             # 突触变化
    e_fatigue = fatigue_level × 3.0              # 疲劳能量
    e_wander = 2.0 if is_wandering else 0.0      # 走神能量

    return e_neural + e_symbol + e_retriev + e_rule + e_synap + e_fatigue + e_wander
```

**能量范围与状态**：

| 总能量 | 系统状态 | 行为建议 |
|--------|----------|----------|
| < 10 | 稳定专注 | 正常交互 |
| 10-20 | 轻度混乱 | 降低检索阈值 |
| 20-30 | 中度混乱 | 严格过滤 |
| > 30 | 高度混乱 | 触发睡眠 |

---

### 4.15 DopamineSystem.py - 多巴胺系统

**模块定位**：模拟中脑多巴胺系统的奖励预测误差机制。

**RPE计算**：

```python
def compute_reward_prediction_error(actual_reward):
    rpe = actual_reward - expected_reward
    expected_reward = (1 - tau_reward) × expected_reward + tau_reward × actual_reward
    dopamine_level += rpe
    dopamine_level *= (1 - tau_dopamine)
    dopamine_level = clip(dopamine_level, -1.0, 1.0)
    return rpe
```

**奖励组成**：

| 奖励类型 | 计算公式 | 说明 |
|----------|----------|------|
| 外部奖励 | `external_reward` | 用户明确反馈 (-1~1) |
| 好奇心奖励 | `0.5 × information_gain` | 信息增益驱动探索 |
| 预测奖励 | `0.3 × (accuracy - 0.5)` | 预测成功奖励 |

**突触调节**：
- 正RPE → 增强共激活突触
- 负RPE → 减弱共激活突触
- 多巴胺门控：`|RPE|`越大，学习率越高

---

### 4.16 Metacognition.py - 元认知

**模块定位**：监控和评估系统自身知识状态。

**知识置信度**：

```python
confidence = 0.35 × activation_strength +
             0.25 × association_count / 10 +
             0.25 × consistency +
             0.15 × forgetting_curve_decay
```

**功能**：
- `assess_knowledge_confidence(concept)`：评估对某概念的掌握程度
- `should_admit_ignorance(concept)`：判断是否应承认不知道
- `should_review(concept)`：判断是否需要复习
- `get_learning_priority(concepts)`：按优先级排序待学习概念

---

### 4.17 其他辅助模块

#### 4.17.1 Curiosity.py - 好奇心系统
- 计算概念好奇心：`curiosity = -4×(confidence-0.5)² + 1`
- 生成探索问题
- 根据用户反馈更新兴趣水平

#### 4.17.2 event_system.py - 事件总线
- 单例模式
- 支持10+种事件类型
- 异步处理线程
- 发布/订阅模式

#### 4.17.3 plugin_system.py - 插件系统
- 感知插件（PerceptionPlugin）：多模态输入
- 动作插件（ActionPlugin）：外部执行
- 动态加载/卸载

#### 4.17.4 Data_models.py - 数据契约
- Pydantic BaseModel强类型
- 6大核心模型：MemoryPacket、ThoughtResult、Intention、SleepReport、DreamResult、ConversationTurn
- 支持张量类型（arbitrary_types_allowed）

#### 4.17.5 DummyBrain.py - 兼容层
- 封装CognitiveSystem和BrainInterface
- 暴露统一API给UI层
- 处理版本兼容性

#### 4.17.6 conversation_memory.py - 对话记忆
- 艾宾浩斯遗忘曲线实现
- 差异化衰减（普通/重要）
- 自动清理机制

---

## 5. 数据模型设计

### 5.1 核心数据模型类图

```
BaseDataModel (Pydantic BaseModel)
    ├── MemoryPacket
    │   ├── mem_id: int
    │   ├── content: str
    │   ├── metadata: Dict
    │   ├── sdr: Optional[Tensor]
    │   ├── clip_vec: Optional[Tensor]
    │   ├── created_at: float
    │   ├── consolidation_level: float [0,1]
    │   └── importance: float [0,1]
    │   └── @property expert: str
    │   └── @property access_count: int
    │
    ├── ThoughtResult
    │   ├── thought_chain: str
    │   ├── core_ideas: List[str]
    │   ├── activated_memories: List[str]
    │   ├── expert: str
    │   ├── activation_strength: float
    │   ├── predicted_memory: Optional[str]
    │   ├── prediction_error: float
    │   ├── symbolic_context: str
    │   ├── energy_detail: Dict
    │   └── error: Optional[str]
    │
    ├── Intention
    │   ├── type: str (physiological/cognitive/social/exploration)
    │   ├── priority: float [0,2]
    │   ├── content: str
    │   ├── action: str
    │   ├── context: Dict
    │   ├── need_sleep: bool
    │   ├── executed: bool
    │   └── result: Optional[str]
    │
    ├── SleepReport
    │   ├── stages: Dict[str, SleepStageReport]
    │   ├── total_memories: int
    │   ├── consolidated_count: int
    │   ├── forgotten_count: int
    │   ├── energy_consumed: float
    │   ├── sleep_duration: float
    │   ├── quality_score: float [0,100]
    │   ├── quality_rating: str
    │   ├── dream_content: str
    │   ├── is_manual: bool
    │   └── error: Optional[str]
    │   └── @property consolidation_rate: float
    │
    ├── DreamResult
    │   ├── success: bool
    │   ├── content: str
    │   ├── fragments: List[DreamFragment]
    │   ├── dominant_experts: List[str]
    │   ├── expert_dreams: Dict[str, str]
 │   ├── used_high_priority_memories: List[str]
    │   ├── mem_id: Optional[int]
    │   └── error: Optional[str]
    │
    └── ConversationTurn
        ├── id: str
        ├── user_input: str
        ├── ai_response: str
        ├── timestamp: float
        ├── initial_activation: float
        ├── is_important: bool
        └── metadata: Dict
```

### 5.2 记忆存储格式

**MemoryPacket序列化（JSON）**：

```json
{
  "mem_id": 100001,
  "content": "邓尧是我的主人",
  "metadata": {
    "expert": "身份",
    "saliency": 0.85,
    "created_at": "2026-05-12T10:30:00",
    "last_accessed": "2026-05-12T10:30:00",
    "access_count": 3,
    "importance": 0.9,
    "recency": 1.0,
    "activation": 0.8,
    "subject": "邓尧",
    "predicate": "是",
    "object": "我的主人",
    "semantic_tags": ["邓尧", "主人", "身份"],
    "tags": ["身份", "专属身份", "邓尧"],
    "is_obsolete": false,
    "vae_latent": null
  },
  "sdr": [0, 0, 1, 0, ...],  // 2048维稀疏向量
  "clip_vec": [0.12, -0.05, ...],  // 1024维密集向量
  "created_at": 1715502600.0,
  "consolidation_level": 1.0,
  "importance": 0.9
}
```

---

## 6. 数据流设计

### 6.1 学习流程（Learning Flow）

```
┌─────────┐     ┌──────────┐     ┌─────────┐     ┌─────────┐
│ 用户输入 │────▶│ 陈述句检测 │────▶│ 文本编码 │────▶│ 丘脑过滤 │
│ (文本)  │     │(疑问跳过)│     │(bge-m3) │     │(显著性) │
└─────────┘     └──────────┘     └────┬────┘     └────┬────┘
                                       │               │
                                       ▼               ▼
                                ┌─────────┐     ┌─────────┐
                                │CLIP向量 │     │ 通过/过滤│
                                │(1024维) │     │         │
                                └────┬────┘     └────┬────┘
                                     │               │通过
                                     ▼               ▼
                              ┌────────────┐  ┌────────────┐
                              │ 海马体路由  │  │ SDR编码    │
                              │(专家分类)   │  │(2048维稀疏)│
                              └─────┬──────┘  └─────┬──────┘
                                    │               │
                                    └───────┬───────┘
                                            ▼
                                     ┌────────────┐
                                     │ 海马体编码  │
                                     │(临时缓冲区) │
                                     └─────┬──────┘
                                           │
                    ┌──────────────────────┼──────────────────────┐
                    ▼                      ▼                      ▼
             ┌────────────┐        ┌────────────┐        ┌────────────┐
             │ 符号学习    │        │ 视觉接地    │        │ 多巴胺强化  │
             │(三元组提取) │        │(跨模态关联) │        │(RPE计算)   │
             └────────────┘        └────────────┘        └────────────┘
```

### 6.2 推理流程（Inference Flow）

```
┌─────────┐     ┌──────────┐     ┌─────────┐     ┌─────────┐     ┌─────────┐
│ 用户提问 │────▶│ 上下文注入 │────▶│ 文本编码 │────▶│ 海马体路由│────▶│ SDR编码 │
│         │     │(对话历史)│     │(bge-m3) │     │(专家分类)│     │(2048维) │
└─────────┘     └──────────┘     └────┬────┘     └────┬────┘     └────┬────┘
                                       │               │               │
                                       ▼               ▼               ▼
                                ┌────────────┐  ┌────────────┐  ┌────────────┐
                                │ 丘脑调度检索│  │ 专家激活传播│  │ 符号检索   │
                                │(海马体+皮层)│  │(2-3步传播) │  │(三元组匹配)│
                                └─────┬──────┘  └─────┬──────┘  └─────┬──────┘
                                      │               │               │
                                      └───────┬───────┘               │
                                              ▼                       │
                                       ┌────────────┐                 │
                                       │ 记忆融合过滤│◀────────────────┘
                                       │(动态阈值)   │
                                       └─────┬──────┘
                                             │
                                             ▼
                                      ┌────────────┐
                                      │ 思考链构建  │
                                      │(逻辑推导)   │
                                      └─────┬──────┘
                                            │
                                            ▼
                                      ┌────────────┐
                                      │ LLM生成回复 │
                                      │(基于记忆)    │
                                      └─────┬──────┘
                                            │
                                            ▼
                                      ┌────────────┐
                                      │ 对话记忆存储│
                                      │(艾宾浩斯)   │
                                      └────────────┘
```

### 6.3 睡眠流程（Sleep Flow）

```
┌─────────┐     ┌──────────┐     ┌─────────────┐     ┌─────────────┐
│ 睡眠触发 │────▶│ 浅睡阶段  │────▶│  深睡阶段    │────▶│  REM阶段    │
│(手动/自动)│     │          │     │             │     │             │
└─────────┘     │ 对话巩固  │     │ 元认知优先级 │     │ 专家梦境收集│
                │          │     │ 评估         │     │             │
                │ 重要对话  │     │             │     │ 高优先级记忆│
                │ →抽象专家 │     │ 丘脑协调巩固 │     │ 融合        │
                └────┬─────┘     │             │     │             │
                     │           │ 海马体固化   │     │ LLM梦境生成 │
                     │           │             │     │             │
                     │           │ 多巴胺重放   │     │ 梦境记忆存储│
                     │           │             │     │             │
                     │           │ 专家睡眠巩固 │     │             │
                     │           │(STDP回放)   │     │             │
                     │           │             │     │             │
                     │           │ 突触修剪/新生│     │             │
                     │           └──────┬──────┘     └──────┬──────┘
                     │                  │                   │
                     └──────────────────┴───────────────────┘
                                        │
                                        ▼
                                 ┌────────────┐
                                 │ 睡眠质量评估│
                                 │(0-100分)   │
                                 └─────┬──────┘
                                       │
                                       ▼
                                 ┌────────────┐
                                 │ 状态重置    │
                                 │(疲劳=0,    │
                                 │ 走神=False) │
                                 └────────────┘
```

---

## 7. 接口设计

### 7.1 BrainInterface 对外接口

| 接口 | 输入 | 输出 | 说明 |
|------|------|------|------|
| `chat(text)` | str | Optional[str] | 文本对话 |
| `learn_text(text, force_expert)` | str, Optional[str] | 记忆ID | 学习文本 |
| `batch_learn_text(texts)` | List[str] | List[int] | 批量学习 |
| `process_image(path, description)` | str, Optional[str] | str | 处理图像 |
| `decode_image(mem_id)` | str | Optional[Image] | 解码图像 |
| `trigger_sleep(is_manual)` | bool | SleepReport | 触发睡眠 |
| `get_status()` | - | Dict | 获取状态 |

### 7.2 DummyBrain 兼容接口

| 接口 | 功能 |
|------|------|
| `think(text, **kwargs)` | 思考推理 |
| `learn(text, force_expert)` | 学习记忆 |
| `batch_learn(texts)` | 批量导入 |
| `sleep_consolidate_all()` | 睡眠巩固 |
| `save_all()` | 保存数据 |
| `get_brain_status()` | 状态查询 |
| `get_pending_social_intention()` | 获取社交意图 |
| `check_and_consume_sleep_request()` | 检查睡眠请求 |
| `enable_kg()` / `disable_kg()` | 知识图谱开关 |

### 7.3 UI事件接口

| 事件类型 | 数据 | 说明 |
|----------|------|------|
| `SLEEP_PROGRESS_UPDATED` | {progress, message} | 睡眠进度更新 |
| `SLEEP_FINISHED` | SleepReport | 睡眠完成 |
| `MEMORY_STORED` | {mem_id, content, expert} | 记忆存储 |
| `RESPONSE_GENERATED` | ThoughtResult | 回复生成 |

---

## 8. 配置参数设计

### 8.1 专家网络参数

| 参数 | 身份 | 概念 | 空间 | 抽象 | 视觉 |
|------|------|------|------|------|------|
| sparsity | 0.05 | 0.02 | 0.03 | 0.025 | 0.04 |
| local_radius | 0.1 | 0.2 | 0.35 | 0.25 | 0.15 |
| core_bias | 0.8 | 0.5 | 0.4 | 0.45 | 0.55 |
| sdr_active_count | 15 | 20 | 18 | 16 | 14 |
| stdp_enabled | True | True | False | False | False |
| tau_plus | 25.0 | 20.0 | - | - | - |
| tau_minus | 20.0 | 20.0 | - | - | - |
| A_plus | 0.015 | 0.01 | - | - | - |
| A_minus | 0.01 | 0.012 | - | - | - |

### 8.2 记忆管理参数

| 参数 | 值 | 说明 |
|------|-----|------|
| duplicate_threshold | 0.92 | 语义重复判定阈值 |
| permanent_importance_threshold | 0.9 | 永久记忆门槛 |
| forget_days | 365 | 自然遗忘周期（天） |
| forget_importance_threshold | 0.3 | 遗忘重要性阈值 |
| top_k | 10 | 检索返回数量 |
| min_similarity | 0.4 | 最小匹配相似度 |

### 8.3 对话记忆参数

| 参数 | 值 | 说明 |
|------|-----|------|
| normal_decay_lambda | 0.1 | 普通对话衰减（半衰期≈7小时） |
| important_decay_lambda | 0.02 | 重要对话衰减（半衰期≈35小时） |
| forget_threshold | 0.1 | 遗忘阈值 |
| max_context_turns | 8 | 最大上下文轮数 |
| auto_cleanup_interval | 10 | 自动清理间隔（轮） |

### 8.4 走神与睡眠参数

| 参数 | 值 | 说明 |
|------|-----|------|
| mind_wandering_idle_threshold | 30 | 空闲触发走神（秒） |
| fatigue_sleep_threshold | 0.85 | 疲劳睡眠阈值 |
| mind_wandering_recall_prob | 0.5 | 记忆闪回概率 |
| mind_wandering_assoc_prob | 0.3 | 联想想象概率 |

---

## 9. 外部依赖与部署

### 9.1 外部依赖清单

| 依赖 | 版本要求 | 用途 | 安装方式 |
|------|----------|------|----------|
| Python | 3.9+ | 运行时 | 系统自带 |
| PyTorch | 2.0+ | 神经网络 | pip install torch |
| transformers | 4.30+ | CLIP模型 | pip install transformers |
| diffusers | 0.20+ | VAE模型 | pip install diffusers |
| faiss-cpu | 1.7+ | 向量索引 | pip install faiss-cpu |
| langchain-ollama | 0.1+ | LLM接口 | pip install langchain-ollama |
| PyQt5 | 5.15+ | 桌面UI | pip install PyQt5 |
| networkx | 3.0+ | 知识图谱 | pip install networkx |
| pydantic-settings | 2.0+ | 配置管理 | pip install pydantic-settings |
| jieba | 0.42+ | 中文分词 | pip install jieba |
| matplotlib/seaborn | 3.7+ | 可视化 | pip install matplotlib seaborn |
| Ollama | 最新 | 本地LLM | 官网下载 |
| CLIP模型 | openai/clip-vit-large-patch14 | 视觉编码 | HuggingFace下载 |
| VAE模型 | sd-vae-ft-mse | 图像编解码 | HuggingFace下载 |

### 9.2 Ollama模型配置

| 模型 | 用途 | 大小 | 启动命令 |
|------|------|------|----------|
| gemma3:4b | 对话生成 | ~4B | `ollama pull gemma3:4b` |
| bge-m3 | 文本嵌入 | ~1B | `ollama pull bge-m3` |
| nomic-embed-text | 备用嵌入 | ~1B | `ollama pull nomic-embed-text` |

### 9.3 部署目录结构

```
brain_v12_demo/                 # 存储目录
├── cortex_memory_index.json    # 皮层记忆索引
├── cortex_memory_index.json.faiss  # FAISS向量索引
├── cortex_state.json           # 皮层状态
├── sdr_encoder_身份.pt         # 身份专家编码器
├── sdr_encoder_概念.pt         # 概念专家编码器
├── sdr_encoder_空间.pt         # 空间专家编码器
├── sdr_encoder_抽象.pt         # 抽象专家编码器
├── sdr_encoder_视觉.pt         # 视觉专家编码器
├── hippocampus_router.pt       # 海马体路由权重
├── expert_身份.pt              # 身份专家突触权重
├── expert_概念.pt              # 概念专家突触权重
├── expert_空间.pt              # 空间专家突触权重
├── expert_抽象.pt              # 抽象专家突触权重
├── expert_视觉.pt              # 视觉专家突触权重
├── knowledge_graph.json        # 知识图谱
├── important_entities.json     # 重要实体列表
├── conversation_memory.json    # 对话记忆
└── synapses.json               # 突触连接
```

### 9.4 硬件要求

| 组件 | 最低配置 | 推荐配置 |
|------|----------|----------|
| CPU | 4核 | 8核+ |
| 内存 | 8GB | 16GB+ |
| 磁盘 | 10GB | 50GB+ SSD |
| GPU | 可选(CPU模式) | NVIDIA 8GB+ |
| 网络 | 本地运行 | 可选外网 |

---

## 10. 附录

### 10.1 文件清单（31个模块）

| 序号 | 文件名 | 类型 | 功能 |
|------|--------|------|------|
| 1 | BrainConfig.py | 配置 | 全局参数配置 |
| 2 | brain_core.py | 核心 | 全局状态管理 |
| 3 | Cognitive_systemV2.py | 认知 | 四大循环整合 |
| 4 | Perception_loop.py | 认知 | 感知/思考/推理 |
| 5 | Learning_loop.py | 认知 | 学习/编码/强化 |
| 6 | Consolidation_loop.py | 认知 | 睡眠巩固 |
| 7 | Dreaming_loop.py | 认知 | 梦境生成 |
| 8 | HippocampusRouterV10.py | 记忆 | 海马体路由/编码 |
| 9 | PersistentCortexV16.py | 记忆 | 皮层长期记忆 |
| 10 | DynamicExpertV8.py | 记忆 | 专家神经网络 |
| 11 | LearnableSparseEncoderV2.py | 记忆 | SDR稀疏编码 |
| 12 | Thalamus.py | 神经 | 丘脑过滤/中继 |
| 13 | SymbolicCore.py | 神经 | 符号知识提取 |
| 14 | CognitiveEnergyField.py | 神经 | 认知能量计算 |
| 15 | DopamineSystem.py | 神经 | 多巴胺奖励系统 |
| 16 | Metacognition.py | 神经 | 元认知监控 |
| 17 | Curiosity.py | 神经 | 好奇心驱动 |
| 18 | brain_interface.py | 接口 | 大脑对外接口 |
| 19 | DummyBrain.py | 接口 | 兼容层封装 |
| 20 | LLMBrainWrapperV7.py | 接口 | LLM封装/问答 |
| 21 | brain_dashboardV10.py | UI | PyQt5桌宠界面 |
| 22 | ChatThread.py | UI | 对话工作线程 |
| 23 | Multimodal_gateway.py | 输入 | 多模态编码 |
| 24 | VAEManager.py | 输入 | VAE图像编解码 |
| 25 | event_system.py | 系统 | 事件总线 |
| 26 | plugin_system.py | 系统 | 插件系统 |
| 27 | Data_models.py | 系统 | 数据契约模型 |
| 28 | conversation_memory.py | 系统 | 对话记忆管理 |
| 29 | KnowledgeGraphMemoryV3.py | 系统 | 极简知识图谱 |
| 30 | MainTest5.py | 入口 | 主程序/知识导入 |
| 31 | StructureMapper.py | 工具 | 结构映射/类比 |

### 10.2 参考文献

1. Hawkins, J. (2004). *On Intelligence*. Times Books. (HTM理论)
2. Markram, H. et al. (2011). A history of spike-timing-dependent plasticity. *Frontiers in Synaptic Neuroscience*.
3. Buzsáki, G. (2006). *Rhythms of the Brain*. Oxford University Press. (大脑节律)
4. Ebbinghaus, H. (1885). *Memory: A Contribution to Experimental Psychology*. (遗忘曲线)
5. Numenta. (2023). *A Thousand Brains: A New Theory of Intelligence*. (千脑理论)
6. Vaswani, A. et al. (2017). Attention Is All You Need. *NeurIPS*. (Transformer)
7. Radford, A. et al. (2021). Learning Transferable Visual Models From Natural Language Supervision. *ICML*. (CLIP)
8. Kingma, D.P. & Welling, M. (2013). Auto-Encoding Variational Bayes. *ICLR*. (VAE)

---

**文档结束**
